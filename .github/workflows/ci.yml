name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: true
  CARGO_INCREMENTAL: false
  CARGO_NET_RETRY: 10
  RUSTUP_MAX_RETRIES: 10

permissions:
  contents: read

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Install Rust toolchains
        run: |
          rustup toolchain install stable --component clippy
          rustup toolchain install nightly --component rustfmt

      - uses: Swatinem/rust-cache@v2

      - name: fmt
        run: cargo +nightly fmt --all -- --check

      - name: clippy
        run: cargo clippy --all-targets --all-features --no-deps -- -D warnings

      - name: doc
        run: env RUSTDOCFLAGS="-D warnings" cargo +nightly doc --no-deps --document-private-items

      - name: test
        run: cargo test --workspace --all-targets

      - name: e2e self-check
        run: cargo run -p lake-cli

  # jj fires no git hooks locally, so Conventional Commits are enforced here.
  commits:
    name: Conventional Commits
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v2

      - name: check commit messages
        run: bun scripts/check-conventional-commit.ts --range "origin/${{ github.base_ref }}..HEAD"
